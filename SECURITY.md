# 安全与隐私

## 支持范围

当前只维护仓库默认分支上的最新版本。项目是本地离线网页，不提供服务器端服务。

## 报告安全问题

如果问题可能泄露成绩截图、读取本机其他文件或执行非预期代码，请不要先公开完整复现数据。可以先通过仓库所有者提供的私下联系方式说明影响范围，再商定安全的复现方式。

普通识别错误请使用 GitHub 的“识别错误”Issue 模板。

## 使用者隐私

- 导入的图片只在当前浏览器页面中处理；
- 项目不包含上传接口或统计服务；
- 导出的 CSV 由浏览器在本机生成；
- 向 Issue 或 Pull Request 上传截图前，仍需主动移除姓名、学号等个人信息。

