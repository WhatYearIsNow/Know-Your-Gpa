# 测试素材

这里的图片用于自动化回归和人工视觉核对。新增素材时请：

1. 删除姓名、学号等个人信息；
2. 使用能够说明场景的文件名；
3. 记录人工确认的课程数、成绩或总学分；
4. 在 JavaScript 或 Python 测试中加入对应断言。

`.txt` 文件保存与截图对应的人工确认数据，图片本身不应被程序修改。

当前素材不含姓名、学号、手机号或邮箱，但包含真实课程名称、课程代码和成绩。公开仓库前，项目所有者仍应确认愿意公开这些信息；如有顾虑，应改用合成或进一步匿名化的数据。
